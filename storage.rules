rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Very permissive rules for development testing
    // Allow any authenticated user to read/write anything
    // WARNING: NOT SECURE - FOR TESTING ONLY
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}